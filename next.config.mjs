/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "img.freepik.com",
      "docs.material-tailwind.com",
      "img.clerk.com",
      "utfs.io",
    ],
  },
};

export default nextConfig;
