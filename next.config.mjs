/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "img.freepik.com",
      "docs.material-tailwind.com",
      "img.clerk.com",
      "utfs.io",
      "via.placeholder.com",
    ],
  },
};

export default nextConfig;
